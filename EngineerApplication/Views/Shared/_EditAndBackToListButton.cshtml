@inject CommonLocalizationService localizer
@model int

    <div class="row">
        <div class="col">
            <button type="submit" class="btn btn-primary form-control" asp-route-id="@Model" >@localizer.Get("Update")</button>
        </div>
        <div class="col">
            <a asp-action="Index" class="btn btn-success form-control">@localizer.Get("BackToList")</a>
        </div>
    </div>