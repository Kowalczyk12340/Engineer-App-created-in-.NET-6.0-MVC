@page
@inject CommonLocalizationService localizer
@model AccessDeniedModel
@{
    ViewData["Title"] = @localizer.Get("AccessDenied");
}

<header>
    <h1 class="text-danger">@ViewData["Title"]</h1>
    <p class="text-danger">@localizer.Get("WithoutAccess").</p>
</header>
